.card
  .card-content
    支払い方法
  .card-list
    登録クレジットカード情報
    .list-card-info
      = "**** **** **** " + @card_information.last4
    .list-card-days
      - exp_month = @card_information.exp_month.to_s
      - exp_year = @card_information.exp_year.to_s.slice(2,3)
      = exp_month + " / " + exp_year
    .list-card-delete
      =link_to delete_cards_path, {method: :post, class: 'charge-form',  name: "inputForm"} do
        %input{ type: "hidden", name: "card_id", value: "" }
        %p
          削除する


      -# = form_tag(delete_cards_path, method: :post, id: 'charge-form',  name: "inputForm") do
      -#   %input{ type: "hidden", name: "card_id", value: "" }
      -#     %button 削除する



-# // =form_tag(cards_path, method: :get, class:"new") do
-# //   .mypage-main.col-9
-# //     %h2.header_title
-# //       支払い方法
-# //     .single-container
-# //       %section.card_section
-# //         %h3 クレジットカード一覧
-# //         - if @card.present?
-# //           .container
-# //             .card-info
-# //               %p.card-info__number
-# //                 = "**** **** **** " + @card_info.last4 #クレジットカードの下４桁を表示
-# //               %p.card-info__period 
-# //               = @exp_month + " / " + @exp_year
-# //               = button_to "削除する", card_path(@card), method: :delete, class:"card-info__delete"
-# //         - else
-# //           .new-card
-# //             = button_to "クレジットカードを追加する", new_card_path(@card), method: :show, class:"list"
